import { OrbitControls } from "@react-three/drei";

const configGlobal = {
    canvasBackgraundColor: "#002",
    spriteBackgroundColor: "#002",
    cameraPosition: [200, 0, 0]as [number, number, number],
    cameraMaxDistance: 400,
    cameraMinDistance: 10,
    nodeClickDistance: 70,

};

export default configGlobal;

